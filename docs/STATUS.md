# 进度与验证记录

## 2026-09-16：共同基础版

已实现 A 的整数通道、图像 IO、区域清单校验、检测指标和拼图；B 的像素差、Depth-2 树、AdaBoost、三级 Cascade、早拒绝、金字塔/滑窗/NMS；C 的局部像素差、多级可见点 Ridge 回归、NME、统一接口和命令行输出。

共享基础训练入口支持真实清单；合成检查生成自身数据，训练并导出模型。未下载真实数据。

已验证：

- 5 项测试通过：通道与逐像素参考一致、裁剪训练与整图扫描特征一致、检测重复匹配与 NMS、Depth-2 表达 XOR、可见点 NME 与坐标变换。
- 合成训练、导出/加载一致、检测非空、28 点输出、小图空结果、JSON 保存通过。
- 命令行 Demo 输出一个合成检测结果和图片/JSON，通道拼图成功生成并完成视觉检查。
- 合成点是椭圆坐标，没有真实五官语义，结果不可用于课程报告。

验证环境：本机 Python 3.9、NumPy 1.20.3、OpenCV 4.11.0。README 推荐的 Python 3.10 与 requirements 新环境尚未在本机单独复测。

## 未完成

- 真实数据来源与获取、按原图分组划分工具、数据增强。
- 要求的 6 张真实类型通道图和观察分析。
- 真实 28 点数据、编号图、预标注/人工修正工具、测试集人工检查。
- 真实数据训练、完整评估入口、真实 P/R/F1/NME 与困难负样本挖掘。
- 每 Stage 时间/误拒/误放、步长尺度实验、整数浮点一致性分析。
- 最终数据集、真实模型共享、课程报告和交付材料。

## 基础版限制

- 随机候选池和阈值搜索较小，逐窗口 Python 扫描较慢，不保证真实检测效果与速度。
- 弱树最大深度为 2，纯类别分支可能提前成为叶子。固定三个判断节点的导出表达可由 B 后续完善。
- 微型样例负样本耗尽后会重用完整训练池，并记录 reused_training_pool；可能产生重复阶段，不等价于困难负样本挖掘，正式训练需替换此回退。
- 阈值保留当前验证正样本，尚无目标误报率控制。
- 关键点每点仅两对局部采样，三级回归，没有多初始化；真实点序仍需确定。
- 真实数据版本、完整超参数记录和模型说明待补。

后续更新应区分代码实现、合成验证、真实实验，并记录证据。
