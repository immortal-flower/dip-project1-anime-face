# 项目一：从手工特征理解动漫人脸检测与关键点回归

提到人脸检测，很多同学首先想到的是深度神经网络：卷积、特征金字塔、Anchor、Transformer。似乎只有足够大的模型，才能完成足够难的视觉任务。

但项目要展示一个更有启发性的事实：

> **一张人脸并不一定要先被“深度理解”，也可以先被一组非常便宜、非常稳定的几何与灰度关系识别出来。**

参考工程 [cpu-face-hpc](https://github.com/inlmouse/cpu-face-hpc) 给出的核心经验是：在正面或近正面人脸检测中，**11 个由灰度、局部平滑和不同方向像素差构成的手工特征通道**，配合 **Depth-2 AdaBoost Cascade**，已经可以获得很好的人脸检测效果。更值得注意的是，这些特征几乎只涉及整数加法、减法和除以 2；除以 2 在计算机中还可以进一步变成位移运算。当然，传统Cascade算法其效果自然是无法与现代DNN相比拟的，但是对于低功耗设备甚至没有FPU的处理器（例如Cortex-M0/M3/M4等）上仍有重要意义。

这意味着：

- 特征本身非常便宜；
- 每个特征都可以被画图解释；
- 每个弱分类器都知道自己在比较哪两个位置；
- Cascade 的每一次拒绝都可以被追踪；
- 后续迁移到 C++ 时，非常容易做缓存、SIMD、定点化和多线程优化。

动漫人脸通常具有更清晰的轮廓、更强对比的眼睛和更规则的线条。因此，一个自然的假设是：

> **如果连真实照片中的正面人脸都可以由这类简单特征识别，那么结构更鲜明、纹理更简洁的近正面动漫人脸，可能更容易被这类特征捕获。**

但“动漫更简单”不能直接当作结论。动漫人脸也有夸张眼型、发型遮挡、侧脸、倾斜、画风差异和对白框干扰。项目要求你亲手验证：这些简单整数特征究竟在动漫图像上有多强、会在哪些情况下失效，以及为什么失效。

因此，这不是一个“调用模型 API”的实验，而是一个从像素出发理解视觉系统的实验。完成以后，你应该能够向别人解释：

1. 为什么 11 个简单通道可以表达人脸结构；
2. 为什么比较两个像素点的差值就能构成弱分类器；
3. 为什么 AdaBoost 能把很多弱判断组合成强分类器；
4. 为什么 Cascade 能极大减少计算量；
5. 为什么这套算法特别适合进一步做 CPU 高性能优化。

---

## 一、项目概述

项目实现一个面向动漫图像的人脸视觉分析系统，包含两个核心模块：

1. **基于 11 通道手工特征与 Depth-2 AdaBoost Cascade 的动漫人脸检测器**；
2. **基于形状回归思想的动漫人脸 28 点关键点回归器**。

端到端流程如下：

```text
输入动漫图像
    ↓
灰度化
    ↓
构建图像金字塔
    ↓
计算 11 个手工特征通道
    ↓
24 × 24 滑动窗口
    ↓
Depth-2 AdaBoost Cascade
    ↓
候选框聚类 / NMS
    ↓
动漫人脸 Bounding Box
    ↓
人脸区域归一化
    ↓
28 点形状回归
    ↓
结果可视化与导出
```

项目的主线尽量向 `cpu-face-hpc` 的实现靠拢：

- 使用 `24 × 24` 检测窗口；
- 使用 11 个手工特征通道；
- 使用像素差作为弱分类器输入；
- 使用 Depth-2 决策树作为弱学习器；
- 使用多阶段 Cascade 进行早拒绝；
- 使用候选框聚类或 NMS 合并重复检测；
- 使用局部像素差和逐步形状更新完成关键点回归。

---

## 二、为什么从 cpu-face-hpc 开始

### 2.1 它把“传统算法”重新变成了一个值得研究的问题

很多同学会误以为传统视觉算法只是深度学习出现之前的过渡方案，因此只需要记一个名词：Haar、HOG、SVM、AdaBoost。但真正重要的不是名词，而是这些算法背后的工程直觉：

```text
怎样用尽可能少的计算，
从图像中提取足够稳定的结构信息？
```

`cpu-face-hpc` 的价值就在于，它没有把传统检测器当成过时黑盒，而是把它拆成可以观察、可以理解、可以优化的几个部分：

```text
便宜的整数特征
    ↓
非常浅的决策树
    ↓
逐级早拒绝的 Cascade
    ↓
对 CPU 缓存和指令流水友好的数据组织
```

在本项目中，我们只关注前三部分。

### 2.2 它证明“简单特征”不等于“弱特征”

一个像素差特征当然很弱。例如：

$$
f(\mathbf{x}) = C_c(p_1) - C_c(p_2)
$$

单独看，它只能回答一个非常局部的问题：

> 某个通道中，位置 $p_1$ 是否比位置 $p_2$ 更亮？

但人脸并不是随机像素集合。眼睛、鼻梁、嘴角、脸部轮廓之间存在稳定的空间关系。很多个“弱问题”按照合理顺序组合起来，就可以形成强判断：

```text
这里是不是比周围暗？
    ↓
左右两侧是否存在近似对称的暗区？
    ↓
中间是否出现鼻梁方向的结构？
    ↓
下方是否有嘴部边缘？
    ↓
这些结构是否出现在同一张脸的合理位置？
```

项目不要求你一开始就设计完美特征。相反，它希望你观察：一组简单、便宜、可解释的像素关系，如何逐渐组合成可用的人脸检测器。

---

## 三、项目实验目标

完成项目后，你应能够：

1. 从灰度图构建 11 个手工特征通道，并解释每个通道的含义；
2. 说明局部平滑为什么能抑制噪声，像素差为什么具有一定亮度鲁棒性；
3. 使用像素差构建 Depth-2 弱分类器；
4. 使用 AdaBoost 或等价集成方法组合弱分类器；
5. 构建多阶段 Cascade，并解释“早拒绝”为什么能节省计算；
6. 将真实人脸上的特征设计迁移到动漫人脸；
7. 构建动漫人脸正负样本，并完成困难负样本挖掘；
8. 实现 28 点动漫人脸形状回归；
9. 使用 Precision、Recall、F1、IoU、NME 等指标评价系统；
---

## 四、核心原理一：11 个手工特征通道

### 4.1 从一张灰度图开始

设输入灰度图为 $G$，第一个通道就是原图：

$$
C_0[y,x] = G[y,x]
$$

它保留最基本的明暗信息。动漫图像中的眼睛、轮廓、头发和背景通常具有明显灰度差异，因此原图本身就是有价值的信息来源。

但只使用原图有两个问题：

- 单点噪声会直接影响判断；
- 相邻尺度上的结构不容易被统一描述。

因此需要引入平滑通道和梯度通道。

### 4.2 通道 1：2×2 局部均值

第二个通道对原图做局部平均：

$$
C_1[y,x] =
\operatorname{avg}
\left(
C_0[y,x],
C_0[y+1,x],
C_0[y,x+1],
C_0[y+1,x+1]
\right)
$$

它的意义不是“让图像变模糊”，而是让单个像素点代表一个小邻域。这样可以减弱绘制噪声、扫描噪声和局部抖动的干扰。

在教学实现中可以写成整数平均：

```text
(a + b + 1) // 2
```

除以 2 只是整数位移的数学表达，不需要复杂浮点运算。

### 4.3 通道 2：跨 2 像素间隔的平滑

第三个通道在 $C_1$ 上继续组合四个相隔 2 像素的位置：

$$
C_2[y,x] =
\operatorname{avg}
\left(
C_1[y,x],
C_1[y+2,x],
C_1[y,x+2],
C_1[y+2,x+2]
\right)
$$

如果说 $C_1$ 关注局部稳定性，那么 $C_2$ 就开始捕获稍大一点的结构。例如眼睛不再只是一个黑点，而是一个有空间范围的暗区；鼻梁也不再只是孤立像素，而是一个方向性区域。

### 4.4 通道 3～6：短距离方向梯度

在 $C_1$ 上计算四个方向的短距离像素差：

$$
C_3[y,x] = N(C_1[y,x+2] - C_1[y,x])
$$

$$
C_4[y,x] = N(C_1[y+2,x] - C_1[y,x])
$$

$$
C_5[y,x] = N(C_1[y+2,x+2] - C_1[y,x])
$$

$$
C_6[y,x] = N(C_1[y+2,x] - C_1[y,x+2])
$$

其中 $N(d)$ 把差值从 $[-255,255]$ 映射到 $[0,255]$：

$$
N(d) = \left\lfloor\frac{d + 255}{2}\right\rfloor
$$

四个通道分别近似描述：

| 通道 | 主要方向 | 可以观察到的结构 |
|---:|---|---|
| 3 | 水平方向 | 眼角、鼻翼、嘴角、左右边缘 |
| 4 | 垂直方向 | 眉毛、眼睑、嘴唇、下巴轮廓 |
| 5 | 主对角方向 | 斜向脸部轮廓和眼部边界 |
| 6 | 副对角方向 | 另一侧斜向轮廓和对称结构 |

为什么要用差值而不是绝对亮度？因为同一角色在不同画面中可能整体偏亮或偏暗，但“眼睛比眼白暗”“线条比皮肤暗”这种相对关系通常更稳定。

### 4.5 通道 7～10：长距离方向梯度

在 $C_2$ 上计算跨越 4 像素的长距离差值：

$$
C_7[y,x] = N(C_2[y,x+4] - C_2[y,x])
$$

$$
C_8[y,x] = N(C_2[y+4,x] - C_2[y,x])
$$

$$
C_9[y,x] = N(C_2[y+4,x+4] - C_2[y,x])
$$

$$
C_{10}[y,x] = N(C_2[y+4,x] - C_2[y,x+4])
$$

短距离梯度更像“边缘探测器”，长距离梯度更像“结构关系探测器”。对于人脸检测，两者都很重要：

- 短距离梯度发现局部边缘；
- 长距离梯度判断这些边缘是否按人脸结构组织。

### 4.6 11 个通道总览

| 通道 | 内容 | 主要作用 |
|---:|---|---|
| 0 | 原始灰度 | 保留明暗与整体结构 |
| 1 | 2×2 局部均值 | 抑制单点噪声 |
| 2 | 跨 2 像素平滑 | 提取稍大范围结构 |
| 3 | 短距离水平差 | 水平边缘与左右结构 |
| 4 | 短距离垂直差 | 垂直边缘与上下结构 |
| 5 | 短距离主对角差 | 斜向边缘 |
| 6 | 短距离副对角差 | 另一方向斜向边缘 |
| 7 | 长距离水平差 | 较大范围水平结构 |
| 8 | 长距离垂直差 | 较大范围垂直结构 |
| 9 | 长距离主对角差 | 较大范围斜向结构 |
| 10 | 长距离副对角差 | 较大范围反向斜向结构 |

### 4.7 为什么它们“便宜但有表达力”

这 11 个通道不是学习出来的卷积特征图，而是人工定义的图像变换。它们只需要：

- 读取像素；
- 整数加法；
- 整数减法；
- 除以 2；
- 截断到有效范围。

从现代 CPU 的角度看，这些操作非常便宜。更重要的是，每个通道都围绕“局部平滑”和“方向差分”展开，天然适合描述动漫图像中的线条、边缘和明暗关系。

**项目的第一个关键任务不是训练模型，而是把这 11 张通道图画出来。**你需要观察：

- 动漫眼睛在哪些通道中最突出；
- 面部轮廓在短距离还是长距离梯度中更清楚；
- 头发、对白框和背景纹理会在哪些通道中造成干扰；
- 原图通道与梯度通道分别提供了什么互补信息。

---

## 五、核心原理二：像素差如何变成弱分类器

### 5.1 一个弱分类器只问一个简单问题

对于某个 $24 \times 24$ 窗口，选择一个特征通道 $c$ 和两个相对位置 $p_1,p_2$，计算：

$$
f = C_c[p_1] - C_c[p_2]
$$

然后与阈值 $\theta$ 比较：

```text
如果 f > θ：
    走向一个分支
否则：
    走向另一个分支
```

这个问题本身非常弱。例如它可能只是问：

> 左眉附近的灰度是否比其下方区域更低？

或者：

> 右眼外侧的水平梯度是否比内侧更强？

单独一个问题无法判断人脸，但它可以捕获一个有意义的局部关系。

### 5.2 为什么使用 Depth-2 决策树

如果只用一个像素差，就是决策桩；如果再接一个后续判断，就形成 Depth-2 决策树：

```text
                 根节点：比较 f1 与 θ1
                 /                    \
           f1 ≤ θ1                    f1 > θ1
             ↓                          ↓
     左子节点：比较 f2L          右子节点：比较 f2R
        /        \                  /        \
    分数 A      分数 B           分数 C      分数 D
```

Depth-2 的价值在于，它可以表达一种非常朴素的条件关系：

> 如果第一个位置关系成立，再检查第二个位置关系；如果第一个关系不成立，则检查另一个补充关系。

这比单个阈值更灵活，同时又比深层树更容易理解和优化。每棵树只有三个判断节点和四个输出分数，非常适合后续 C++ 重排、预计算和向量化。

### 5.3 AdaBoost 如何把弱判断变成强判断

AdaBoost 的思想可以直观理解为：

1. 先让所有训练样本具有相同权重；
2. 找一棵当前效果最好的 Depth-2 弱树；
3. 提高被它分错样本的权重；
4. 下一棵弱树更关注这些难分样本；
5. 重复多轮，把弱树分数加权累加。

某个阶段的总分可以写成：

$$
S(\mathbf{x}) = \sum_{t=1}^{T} s_t(\mathbf{x})
$$

如果总分超过该阶段阈值，就认为窗口通过当前阶段。

你不需要把 AdaBoost 写成数学证明题，但必须讲清楚：**每一棵弱树为什么被选中，以及它如何纠正前面弱树的错误。**

---

## 六、核心原理三：Cascade 为什么快

### 6.1 大多数窗口不是人脸

在一张图像中，绝大多数滑动窗口都是背景、头发、衣服、文字或物体。如果每个窗口都执行完整分类器，计算量会非常大。

Cascade 的基本思想是：

> 用很便宜的前几级快速拒绝明显不是人脸的窗口，把昂贵计算留给少数可疑窗口。

例如：

```text
窗口
  ↓
Stage 0：只做少量判断
  ├─ 明显不像 → 立即拒绝
  ↓ 通过
Stage 1：做更多判断
  ├─ 不像 → 拒绝
  ↓ 通过
Stage 2：进一步确认
  ├─ 不像 → 拒绝
  ↓ 通过
后续 Stage：最终判断
```

### 6.2 为什么这符合动漫图像

动漫页面中大量区域是平涂背景、对白框、速度线或衣物纹理。这些区域往往在前几级就会暴露出“没有双眼结构”“没有脸部轮廓”“灰度关系不稳定”等特征。因此，Cascade 可以尽早把它们排除。

实验中你必须统计每一级：

- 输入窗口数；
- 通过窗口数；
- 拒绝率；
- 阶段平均耗时；
- 误拒的真实人脸数；
- 误放的背景窗口数。

这些数据会帮助你理解：Cascade 的速度优势不是凭空出现的，而是用“尽早做粗判断”换来的。

---

## 七、数据集与任务定位

### 7.1 数据来源

检测任务主要使用：

- [AnimeFace Dataset](https://www.kaggle.com/datasets/splcher/animefacedataset)：动漫人脸图像（正样本）与关键点数据池。
- [Manga109](https://manga109.github.io/)：完整漫画页面与人脸 Bounding Box（需申请，作为可选补充）；

关键点初始标注可参考：

- [anime-face-detector](https://github.com/hysts/anime-face-detector)：提供动漫人脸框与 28 点关键点的参考输出。



## 八、项目步骤一：环境搭建（uv 创建 Python 环境并安装依赖）

### 8.1 安装 uv

uv 是一个用 Rust 编写的高性能 Python 包管理器（pip 的现代替代），安装与依赖解析速度都比 pip 快数倍。安装方式：

```powershell
# Windows PowerShell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

```bash
# macOS / Linux
curl -LsSf https://astral.sh/uv/install.sh | sh
```

安装后验证：

```bash
uv --version
```

### 8.2 创建 Python 虚拟环境

在项目目录（`experiment1_animeface/`）下创建虚拟环境：

```bash
uv venv .venv --python 3.10
```

- `uv venv` 会自动下载并安装指定版本的 Python，无需预先安装；
- 若本机已有合适版本，可省略 `--python` 参数直接使用。

激活环境：

```powershell
# Windows PowerShell
.venv\Scripts\Activate.ps1
```

```bash
# macOS / Linux
source .venv/bin/activate
```

激活后命令行提示符前缀出现 `(.venv)`，即表示已进入虚拟环境。

### 8.3 安装依赖包

方式一：按 `requirements.txt` 安装（推荐，便于复现与提交）：

```bash
uv pip install -r requirements.txt
```

`requirements.txt` 建议内容（项目用到的主要依赖）：

```
numpy>=1.24
opencv-python>=4.8
scikit-learn>=1.3
matplotlib>=3.7
pillow>=10.0
tqdm>=4.66
```

方式二：直接按包名安装：

```bash
uv pip install numpy opencv-python scikit-learn matplotlib pillow tqdm
```

### 8.4 验证环境

```bash
uv run python -c "import numpy, cv2, sklearn, matplotlib; print('environment ok')"
```

或运行项目命令确认可导入。**本周产出**：环境截图（`uv --version` 与依赖安装成功的输出）。

---

## 九、项目步骤二：复现并观察 11 通道特征

### 9.1 基础实现

实现函数：

```python
def compute_11_channels(gray: np.ndarray) -> list[np.ndarray]:
    """输入 uint8 灰度图，返回 11 个 uint8 特征通道。"""
    pass
```

要求：

- 中间计算使用 `int16` 或 `int32`，避免溢出；
- 最终结果保存为 `uint8`；
- 边界无法计算的位置可以置 0，但必须统一处理；
- 不使用现成卷积库直接生成全部结果；
- 代码应能清楚看出“哪个像素减去哪个像素”。

### 9.2 可视化任务

选择至少 6 张图像：

- 2 张真实正面人脸；
- 2 张近正面动漫人脸；
- 1 张带头发遮挡的动漫人脸；
- 1 张漫画复杂背景。

绘制：

```text
原图 + 11 个通道
```

并回答：

1. 哪些通道最能突出眼睛；
2. 哪些通道最能突出脸部轮廓；
3. 哪些通道最容易被头发或对白框干扰；
4. 动漫人脸与真实人脸在通道图上有哪些相似和不同。

这一部分不是“交几张图”就结束。你需要用观察结果解释：**为什么后面 Cascade 可以从这些通道中选到有效弱分类器。**

---

## 十、项目步骤三：构建动漫人脸检测训练集

### 10.1 正样本

从 Manga109 或教师提供的数据中裁剪人脸，并缩放到 `24 × 24`：

```text
人脸 Bounding Box
    ↓
适度扩展边界
    ↓
裁剪
    ↓
resize 到 24 × 24
    ↓
计算 11 通道
```

建议正样本覆盖：

- 不同脸型；
- 不同眼型；
- 不同发型；
- 轻微旋转；
- 不同绘制风格；
- 少量遮挡样本。

### 10.2 负样本

负样本必须来自真实动漫场景，而不能只使用随机噪声。建议包括：

- 漫画背景；
- 头发；
- 身体与衣服；
- 手部；
- 文字框；
- 拟声词；
- 建筑物和物体；
- 与人脸局部相似但并非完整人脸的区域。

负样本同样缩放到 `24 × 24` 并计算 11 通道。

### 10.3 数据划分

建议：

```text
Train       75%
Validation  10%
Test        15%
```

必须避免数据泄漏：

- 同一原图的不同裁剪版本不能跨训练集和测试集；
- 同一漫画页面采样的大量相似负样本不能同时出现在训练和测试中；
- 数据增强只允许作用于训练集；
- 保存固定划分文件和随机种子。

---

## 十一、项目步骤四：训练 Depth-2 AdaBoost Cascade

### 11.1 候选弱特征定义

一个候选像素差特征定义为：

```python
Feature = {
    "channel": c,
    "p1": (x1, y1),
    "p2": (x2, y2)
}
```

对应计算：

$$
f = C_c[y+y_1, x+x_1] - C_c[y+y_2, x+x_2]
$$

一棵 Depth-2 弱树包含：

- 根节点特征与阈值；
- 左子节点特征与阈值；
- 右子节点特征与阈值；
- 四个叶子输出分数。

### 11.2 训练的基本思路

为了控制计算量，可以采用随机候选 + 贪心搜索：

1. 随机生成一组候选根节点特征；
2. 为每个根节点选择最优阈值；
3. 在左右分支分别搜索子节点；
4. 选择加权错误率最低的一棵 Depth-2 树；
5. 用 AdaBoost 更新样本权重；
6. 重复选择多棵弱树，形成一个 Stage；
7. 按目标通过率组织多个 Stage，形成 Cascade。

教师可以提供简化训练框架，但学生必须解释每个模块的作用，不能只会调用训练脚本。

### 11.3 Cascade 训练建议

至少构建 3 个 Stage。每个 Stage 可以设置：

- 本阶段目标召回率；
- 本阶段最大误报率；
- 最大弱树数量。

训练完成后统计：

```text
Stage 0：保留多少窗口
Stage 1：保留多少窗口
Stage 2：保留多少窗口
```

如果第一级就拒绝大量真实人脸，说明阈值过严或弱分类器太弱；如果最后仍有大量背景通过，说明负样本不足或后续 Stage 判别能力不足。

### 11.4 困难负样本挖掘

完成初始检测器后，在训练图像上运行完整扫描：

```text
初始检测器
    ↓
扫描漫画页面
    ↓
收集高置信度误检
    ↓
加入负样本
    ↓
重新训练
```

至少展示 6 个困难负样本，并解释它们为什么容易被误认为动漫人脸。例如：

- 两个深色图案形成类似双眼的结构；
- 文字框边缘形成类似脸轮廓的结构；
- 头发纹理形成类似眼部线条的结构。

---

## 十二、项目步骤五：多尺度搜索与候选框合并

### 12.1 图像金字塔

由于训练窗口固定为 `24 × 24`，要检测不同大小的人脸，需要缩放原图：

```text
原图
  ↓
缩小一层
  ↓
再缩小一层
  ↓
……
```

推荐从参考实现的思想出发，使用约 `1.2` 的尺度增长关系。你也可以比较 `1.1、1.2、1.3`，但必须记录每层：

- 缩放后尺寸；
- 滑窗数量；
- 通过 Cascade 的候选数；
- 处理时间。

### 12.2 滑动窗口

基础版本从逐像素扫描开始，保证结果正确：

```text
step = 1
```
也即是滑动窗口逐像素滑动。

然后再比较：

```text
step = 2
```

量化研究步长大小对结果的影响。

### 12.3 候选框聚类或 NMS

同一张脸可能在相邻位置和相邻尺度产生多个候选框。可以使用两种方式之一：

1. NMS：按分数排序并抑制重叠框；
2. Similar Rectangles + 聚类：将位置和大小相近的框合并，并按邻居数过滤。

推荐先理解参考工程中的“相似框聚类”思想：相邻尺度、相邻位置对同一张脸给出的多个框，不应当被看成多张脸，而应当合并成一个更稳定的结果。

至此，人脸检测部分已经完成。

---

## 十三、项目步骤六：28 点关键点回归

### 13.1 为什么关键点不适合只靠一次分类

检测器回答的是“脸在哪里”，关键点回归回答的是“眼睛、眉毛、鼻尖、嘴角分别在哪里”。人脸检测框只能给出一个粗位置，无法描述五官形变。

因此需要形状回归：

```text
初始平均形状
    ↓
根据当前形状采样局部像素差
    ↓
预测形状残差
    ↓
更新关键点
    ↓
重复若干轮
    ↓
得到 28 点
```

这与参考工程中 LBF / Explicit Shape Regression 的思想一致：不是一次性从整张脸猜出所有点，而是根据当前形状反复修正。

### 13.2 28 点标注格式

每个样本固定包含 28 个点：

```json
{
  "image": "images/000001.jpg",
  "width": 512,
  "height": 512,
  "bbox": [96, 72, 416, 392],
  "landmarks": [
    {"x": 123.4, "y": 156.2, "visibility": 1}
  ]
}
```

要求：

- `landmarks` 长度必须为 28；
- 点顺序必须固定；
- 被遮挡或不可见的点使用 `visibility = 0`；
- 提交一张编号示意图；
- 测试集必须人工检查。

注意，现目前**没有现成28点的人脸数据集**，同学可以使用[这个工程](https://github.com/hysts/anime-face-detector)中的模型进行预标注，再人工修正模型标注后的结果，实现一个半自动化的标注流水线。

### 13.3 基础形状回归方案

推荐实现一个简化版多级回归：

1. 根据检测框生成归一化平均形状；
2. 围绕每个当前关键点采样若干像素差特征；
3. 将所有特征拼接为向量；
4. 使用 Ridge Regression 预测 56 维形状残差；
5. 更新关键点；
6. 重复 3～5 轮。

第 $k$ 轮更新可以写成：

$$
\mathbf{s}_{k+1} =
\mathbf{s}_{k} + W_k\phi(I,\mathbf{s}_{k})
$$

其中：

- $\mathbf{s}_{k}$ 是当前 28 点形状；
- $\phi(I,\mathbf{s}_{k})$ 是依赖当前形状位置的局部像素差特征；
- $W_k$ 是第 $k$ 轮学习到的回归矩阵。

### 13.4 进阶方案

可进一步实现 Fern / LBF 风格特征：

```text
当前关键点位置
    ↓
采样多个像素差
    ↓
二值编码或 Fern 叶节点
    ↓
局部二值特征
    ↓
线性回归更新形状
```

挑战内容可以包括：

- 每个关键点使用多个局部偏移；
- 多级形状更新；
- 可见点加权损失；
- 与 HOG + 回归或轻量 CNN 的对比。

---

## 十四、评价指标

### 14.1 检测指标

Intersection over Union：

$$
\mathrm{IoU} =
\frac{|B_p \cap B_g|}{|B_p \cup B_g|}
$$

基础实验建议使用：

$$
\mathrm{IoU} \ge 0.5
$$

判断预测框是否匹配真实框。

Precision：

$$
P = \frac{TP}{TP + FP}
$$

Recall：

$$
R = \frac{TP}{TP + FN}
$$

F1：

$$
F_1 = \frac{2PR}{P+R}
$$

### 14.2 关键点指标

使用 Normalized Mean Error：

$$
\mathrm{NME} =
\frac{1}{N}
\sum_{i=1}^{N}
\frac{\left\|p_i-\hat p_i\right\|_2}{d}
$$

推荐 $d$ 使用双眼中心距离。如果双眼不可见，可以使用人脸框对角线长度，但必须明确说明。

默认只统计 `visibility = 1` 的点。

### 14.3 运算与效率观察

项目一不追求极限性能，但需要记录：

- 平均每张图检测时间；
- 每个 Stage 通过率；
- 不同步长下的窗口数量；
- Python 整数版与浮点版输出是否一致；

---

## 十五、（可选）探究性实验

### 11 通道是否真的是最优

现目前的11通道是为真实人脸设计的，不一定契合于动漫人脸。所以希望能够探索其他的策略。

比较至少三种策略并实验报告 Precision、Recall、F1 和典型错误图。


### 滑窗步长与尺度系数

比较：

```text
step = 1 与 step = 2
scale = 1.1、1.2、1.3
```

分析窗口数量、Recall、定位偏差和运行时间之间的关系。

### 困难负样本是否有效

比较：

```text
普通随机负样本
    VS
加入 Hard Negative 后重新训练
```

重点观察误报是否下降，尤其是文字框、头发和图案区域。


### 整数特征与浮点实现的一致性

分别实现或模拟：

- 整数 `uint8/int16` 特征；
- 浮点 `float32` 特征。

比较：

- 11 通道最大差异；
- 检测结果差异；
- Landmark NME 差异；
- 推理时间差异。

这项实验将直接为实验二的 Fixed Point 和 SIMD 优化做准备。

---

## 十六、最终 Demo 与统一接口

### 16.1 命令行 Demo

```bash
python demo.py --image test.jpg --model-dir models/ --output results/test_vis.jpg
```

输出：

- 人脸 Bounding Box；
- Detection Score；
- 28 个关键点；
- 结构化 JSON；
- 可选的 11 通道可视化图。

### 16.2 统一 Python 接口

```python
class AnimeFaceDetector:
    def __init__(self, model_path: str):
        """加载 11 通道定义、Cascade、关键点回归器和配置。"""
        pass

    def detect(self, image):
        """输入 OpenCV BGR 图像，返回原始图像坐标下的人脸结果。"""
        pass
```

返回格式：

```python
[
    {
        "bbox": [x1, y1, x2, y2],
        "score": 0.97,
        "landmarks": [
            [x1, y1],
            [x2, y2],
            # ...
            [x28, y28]
        ]
    }
]
```

要求：

- 检测不到人脸时返回空列表；
- 结果按分数从高到低排序；
- 坐标使用原始输入图像坐标；
- 关键点顺序固定；
- 不在 `detect()` 中弹窗或等待人工输入。

---

## 十七、模型导出要求

结束时，必须保存：

```text
1. 11 通道特征定义
2. Depth-2 弱树参数
3. Cascade Stage 阈值
4. 图像金字塔参数
5. 候选框合并参数
6. 关键点回归模型
7. 关键点编号与归一化配置
8. 数据划分与随机种子
```

推荐目录：

```text
models/
├── detector/
│   ├── feature_definition.json
│   ├── depth2_trees.json
│   ├── cascade.json
│   └── weights.npz
├── landmark/
│   ├── mean_shape.npy
│   ├── regressor.json
│   └── weights.npz
├── config.json
└── README.md
```

`config.json` 示例：

```json
{
  "window_size": [24, 24],
  "num_channels": 11,
  "scale_factor": 1.2,
  "step": 1,
  "nms_threshold": 0.3,
  "min_neighbors": 2,
  "landmark_count": 28,
  "landmark_input_size": [128, 128]
}
```

---

## 十八、建议工程结构

```text
experiment1_animeface/
├── data/
│   ├── raw/
│   ├── processed/
│   └── splits/
├── annotations/
│   ├── auto/
│   └── corrected/
├── src/
│   ├── channels11.py
│   ├── pyramid.py
│   ├── sliding_window.py
│   ├── depth2_tree.py
│   ├── adaboost.py
│   ├── cascade.py
│   ├── grouping.py
│   ├── shape_regression.py
│   ├── metrics.py
│   └── detector.py
├── scripts/
│   ├── visualize_channels.py
│   ├── prepare_detection_data.py
│   ├── train_detector.py
│   ├── mine_hard_negatives.py
│   ├── prepare_landmark_data.py
│   ├── train_landmark.py
│   └── evaluate.py
├── models/
├── results/
├── demo.py
├── requirements.txt
└── README.md
```

---


## 十九、提交内容

必须提交：

1. Python 源代码（requirements.txt、README 运行说明、完整的代码）；
2. 构建的人脸28点关键点数据集（图片以及标注）
3. 模型文件与配置文件；
4. 最终Demo（11个特征通道图、最终的检测效果图）；
5. 项目报告；

---


### 加分项

- 完整实现 Similar Rectangles 聚类；
- 完整实现 Hard Negative Mining；
- 实现 LBF / Fern 风格关键点特征；
- 对 11 通道做系统的改进，并佐证以消融实验；
- 对动漫与真实人脸通道响应做深入比较；
- 整数版与浮点版结果高度一致；
......


---


## 参考资料

1. [cpu-face-hpc](https://github.com/inlmouse/cpu-face-hpc)：11 通道手工特征、Depth-2 AdaBoost Cascade、CPU 高性能实现参考。
2. [cpu-face-hpc / face_frontal.py](https://github.com/inlmouse/cpu-face-hpc/blob/master/face_frontal.py)：教学版 Python 参考实现。
3. [Manga109](https://manga109.github.io/)：漫画图像与人脸框数据。
4. [AnimeFace Dataset](https://www.kaggle.com/datasets/splcher/animefacedataset)：动漫人脸图像数据。
5. [anime-face-detector](https://github.com/hysts/anime-face-detector)：动漫人脸 28 点关键点参考。
6. Viola, P., & Jones, M. Rapid Object Detection using a Boosted Cascade of Simple Features.

---

## 附录 A：11 通道实现伪代码

```python
def compute_11_channels(gray):
    ch = [zeros_like(gray) for _ in range(11)]
    ch[0] = gray

    # ch1: 2x2 local average
    ch[1] = average(
        ch[0][y, x], ch[0][y+1, x],
        ch[0][y, x+1], ch[0][y+1, x+1]
    )

    # ch2: smoothed response with a 2-pixel gap
    ch[2] = average(
        ch[1][y, x], ch[1][y+2, x],
        ch[1][y, x+2], ch[1][y+2, x+2]
    )

    # short-range gradients from ch1
    ch[3] = normalize_diff(ch[1][y, x+2] - ch[1][y, x])
    ch[4] = normalize_diff(ch[1][y+2, x] - ch[1][y, x])
    ch[5] = normalize_diff(ch[1][y+2, x+2] - ch[1][y, x])
    ch[6] = normalize_diff(ch[1][y+2, x] - ch[1][y, x+2])

    # long-range gradients from ch2
    ch[7]  = normalize_diff(ch[2][y, x+4] - ch[2][y, x])
    ch[8]  = normalize_diff(ch[2][y+4, x] - ch[2][y, x])
    ch[9]  = normalize_diff(ch[2][y+4, x+4] - ch[2][y, x])
    ch[10] = normalize_diff(ch[2][y+4, x] - ch[2][y, x+4])

    return ch
```

## 附录 B：Depth-2 弱树推理伪代码

```python
def eval_depth2_tree(tree, channels, x, y):
    root = tree.root
    f1 = pixel_diff(channels, root.feature, x, y)

    if f1 > root.threshold:
        node = tree.right_child
    else:
        node = tree.left_child

    f2 = pixel_diff(channels, node.feature, x, y)

    if f2 > node.threshold:
        return node.score_right
    else:
        return node.score_left
```


